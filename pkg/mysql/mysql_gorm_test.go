package mysql

import (
	"testing"
)

func TestInitGorm(t *testing.T) {
	//assert.NotNil(t, Gorm)
	//var r any
	//err := Gorm.Table("vuls").Select("SELECT *").Limit(1).Scan(&r).Error
	//assert.Nil(t, err)
	//fmt.Println(r)
}
